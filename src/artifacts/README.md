# Contract Artifacts

This directory will contain the compiled contract artifacts after running `npx hardhat compile`.

The artifacts should include:
- contracts/GameFactory.sol/GameFactory.json
- contracts/TwoThirdsAverageGame.sol/TwoThirdsAverageGame.json

If you're seeing import errors, make sure to run the compile command first.
